<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Редактивровать", edit_article_path(@article) %></li>
  <li><%= link_to "Удалить", article_path(@article),
                  method: :delete,
                  data: { confirm: "Удалить статью?" } %></li>  
  <li><%= link_to "Вернуться на главную", articles_path %></li>
</ul>
<hr>
<h2>Список комментариев</h2>
<!-- Вместо этого куска
<% @article.comments.each do |comment| %>
  <p>
    <strong>Имя:</strong>
    <%= comment.commenter %>
  </p>

  <p>
    <strong>Комментарий:</strong>
    <%= comment.body %>
  </p>
<% end %>
-->
<!-- Добавим более изящное -->
<%= render @article.comments %>
<hr>
<h2>Добавить комментарий:</h2>
<!-- Так же меняем на более изящный код
<%= form_with model: [ @article, @article.comments.build ] do |form| %>
  <p>
    <%= form.label "Имя" %><br>
    <%= form.text_field :commenter %>
  </p>
  <p>
    <%= form.label "Комментарий" %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit "Добавить"%>
  </p>
<% end %>
-->
<%= render 'comments/form' %>
